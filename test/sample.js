var assert = require("assert");

describe("Something", function(){
  describe("The way it smells", function() {
    it("is pungent", function() {
      assert.ok(true);
    });
    it("is coffee", function() {
      assert.ok(true);
    });
  });
});
